**HLS** 的全名是 **HTTP Live Streaming**。

這是由 **Apple** 於 2009 年推出的**基於 HTTP 協定的動態自適應串流媒體傳輸協定**。

## 核心涵義拆解

- **HTTP**：採用標準的 HTTP / HTTPS 通訊協定（預設 Port 80 / 443）。傳統串流（如 RTMP 或 RTSP）需要特殊伺服器與特定 Port，常被防火牆阻擋；而 HLS 將影片當作「一般的網頁靜態檔案」傳送，不僅能**輕鬆穿透防火牆**，還能直接利用現有的 **Web CDN（內容傳遞網路）** 進行大規模快取與分發。
    
- **Live**：同時支援**即時直播（Live Streaming）**與**隨選視訊（VOD, Video on Demand）**。透過將連續的影片流切成一小段一小段的檔案，實現即時播放。
    
- **Streaming**：支援**自適應碼率（ABR, Adaptive Bitrate Streaming）**。伺服器會同時準備多種畫質（如 1080p、720p、480p），播放器會根據使用者當前的網路頻寬，動態切換畫質，確保播放不卡頓。
    

## HLS 的運作架構

HLS 的運作機制可以簡化為 3 個核心元件：

```
[影片源] ➔ [切片器 (Segmenter)] ➔ [HTTP 伺服器 / CDN] ➔ [客戶端播放器]
                                    ├── 主索引檔 (.m3u8)
                                    └── 媒體切片 (.ts / .m4s)
```

1. **媒體切片（Media Segments）**
    
    影片會被切成數秒長（通常 2~6 秒）的小檔案，常見格式為 `.ts`（MPEG-2 Transport Stream）或現代的 `.m4s` (fMP4)。
    
2. **索引清單（M3U8 Manifest）**
    
    這是一個 UTF-8 編碼的純文字檔（副檔名 `.m3u8`），扮演「導覽地圖」的角色：
    
    - **主播放清單（Master Playlist）**：列出不同畫質與頻寬對應的播放清單網址。
        
    - **媒體播放清單（Media Playlist）**：列出具體每個 `.ts` 切片檔案的播放順序、時間長度與下載連結。
        
3. **播放端（Client Player）**
    
    播放器（如 iOS 原生播放器、瀏覽器端的 `Hls.js`）會先讀取 `.m3u8`，再依序向伺服器請求對應的切片檔案並連續播放。
    

## 優缺點總結

|**特點**|**說明**|
|---|---|
|**優點**|跨平台相容性極佳（iOS、Android、各大瀏覽器全面支援）、防護牆友善、伺服器與 CDN 成本低。|
|**缺點**|**傳統延遲較高**（一般在 6~30 秒之間）。_註：現已有 **LL-HLS（Low-Latency HLS）** 延伸規格將延遲降至 2 秒以內。_|